# AT-50 — full local stack: web, api, worker, renderer, redis
# Usage:
#   1. Copy .env.example → .env and fill Supabase + LLM keys
#   2. docker compose up --build
#
# Supabase: hosted project (recommended) or local Supabase — set credentials in .env only.

name: borek-ai-suite

services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "${REDIS_PUBLISH_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  renderer:
    build:
      context: .
      dockerfile: docker/renderer/Dockerfile
    restart: unless-stopped
    environment:
      RENDERER_PORT: ${RENDERER_PORT:-4000}
      SOFFICE_PATH: ${SOFFICE_PATH:-/usr/bin/soffice}
      PDFTOPPM_PATH: ${PDFTOPPM_PATH:-/usr/bin/pdftoppm}
      RENDERER_MAX_REQUEST_BYTES: ${RENDERER_MAX_REQUEST_BYTES:-10485760}
    ports:
      - "${RENDERER_PUBLISH_PORT:-4000}:4000"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:4000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      REDIS_URL: redis://redis:6379/0
      RENDERER_URL: http://renderer:${RENDERER_PORT:-4000}
      API_DATA_BACKEND: ${API_DATA_BACKEND:-supabase}
      # AI_EXECUTION_MODE and RENDERER_EXECUTION_MODE come from env_file (.env) only.
      # Do not default them here — a compose-level default can hide .env=live and force fixture decks.
      ARTIFACT_ROOT: /artifacts
      API_HOST: 0.0.0.0
      API_PORT: 8000
    volumes:
      - presentation_artifacts:/artifacts
    ports:
      - "${API_PUBLISH_PORT:-8000}:8000"
    depends_on:
      redis:
        condition: service_healthy
      renderer:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 20s

  worker:
    build:
      context: .
      dockerfile: docker/worker/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      REDIS_URL: redis://redis:6379/0
      RENDERER_URL: http://renderer:${RENDERER_PORT:-4000}
      API_DATA_BACKEND: ${API_DATA_BACKEND:-supabase}
      # AI_EXECUTION_MODE and RENDERER_EXECUTION_MODE come from env_file (.env) only.
      # Do not default them here — a compose-level default can hide .env=live and force fixture decks.
      ARTIFACT_ROOT: /artifacts
    volumes:
      - presentation_artifacts:/artifacts
    depends_on:
      redis:
        condition: service_healthy
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "celery -A app.worker inspect ping --timeout 5 2>&1 | grep -q pong || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 45s

  web:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
        NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL:-}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: ${WEB_PORT:-3000}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
    ports:
      - "${WEB_PUBLISH_PORT:-3000}:3000"
    depends_on:
      api:
        condition: service_healthy

volumes:
  presentation_artifacts:
